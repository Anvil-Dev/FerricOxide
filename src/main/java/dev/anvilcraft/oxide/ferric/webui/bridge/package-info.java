/**
 * Two-way messaging between the mod and the page loaded in a WebView: events and queries in
 * both directions, over a single JSON envelope format. See {@code docs/webui-bridge.md}.
 */
@NullMarked
package dev.anvilcraft.oxide.ferric.webui.bridge;

import org.jspecify.annotations.NullMarked;
